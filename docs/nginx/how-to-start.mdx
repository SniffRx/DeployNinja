import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 📦 Установка и настройка Nginx для Linux

<Tabs groupId="os">
    <TabItem value="arch" label="Arch Linux" default>

        ## 🔄 Шаг 1: Обновление системы
        Перед установкой рекомендуется обновить пакеты системы до последних версий:

        <Tabs groupId="package-manager">
            <TabItem value="pacman" label="Pacman" default>
                ```bash
                sudo pacman -Syu
                ```
            </TabItem>
            <TabItem value="yay" label="Yay">
                ```bash
                yay -Syu
                ```
            </TabItem>
        </Tabs>

        > Команда синхронизирует базы пакетов и обновляет все установленные пакеты. Это особенно важно в Arch Linux,
        который является rolling-release дистрибутивом.

        ## 🌐 Шаг 2: Установка Nginx
        Установим веб-сервер Nginx из официального репозитория Arch:

        <Tabs groupId="package-manager">
            <TabItem value="pacman" label="Pacman" default>
                ```bash
                sudo pacman -S nginx
                ```
            </TabItem>
            <TabItem value="yay" label="Yay">
                ```bash
                yay -S nginx
                ```
            </TabItem>
        </Tabs>

        ## ⚙️ Шаг 3: Запуск и включение автозапуска службы
        После установки необходимо запустить службу и добавить её в автозагрузку:
        ```bash
        sudo systemctl start nginx
        sudo systemctl enable nginx
        ```
        * start — немедленный запуск службы.
        * enable — добавление службы в автозагрузку при старте системы.

        ## 📋 Шаг 4: Проверка статуса службы
        Проверим, успешно ли запустился Nginx:
        ```bash
        sudo systemctl status nginx
        ```
        > Вы должны увидеть строку с active (running), если всё работает корректно.

        ## 🌍 Шаг 5: Проверка доступности веб-сервера
        Откройте браузер и перейдите по адресу: `http://localhost`. Если установка прошла успешно, вы увидите стартовую
        страницу Nginx — это подтверждение, что сервер обрабатывает HTTP-запросы.
        ## 🔐 Шаг 6: Настройка брандмауэра (если используется UFW
        Если у вас установлен ufw (Uncomplicated Firewall), откройте порты для HTTP и HTTPS:
        ```bash
        sudo ufw allow http
        sudo ufw allow https
        sudo ufw enable
        ```
        > ⚠️ Обратите внимание: на Arch Linux ufw по умолчанию не установлен. При необходимости установите его:
        sudo pacman -S ufw

        ## 🛠️ Шаг 7: Основные команды управления Nginx
        ```bash
        # 🔁 Перезапуск службы (используется после обновлений)
        sudo systemctl restart nginx

        # ✅ Проверка корректности конфигурации
        sudo nginx -t

        # 🔄 Перезагрузка конфигурации без остановки службы
        sudo systemctl reload nginx

        # ⛔ Остановка службы
        sudo systemctl stop nginx
        ```
        > Рекомендуется всегда проверять конфигурацию командой nginx -t перед перезагрузкой или перезапуском, чтобы
        избежать сбоев.

        ## 📁 Шаг 8: Структура и расположение важных файлов
        | Назначение | Путь | Описание |
        |--------------------------------|---------------------------------------|------------------------------------------------------------------------------|
        | Основной конфигурационный файл | /etc/nginx/nginx.conf | Главный файл настроек Nginx |
        | Каталог конфигураций сайтов | /etc/nginx/conf.d/ | Место для размещения отдельных конфигураций (виртуальных
        хостов) |
        | Логи сервера | /var/log/nginx/access.log и error.log | Логи запросов и ошибок |
        | Публичная директория сайтов | /usr/share/nginx/html/ | Каталог, в котором лежит стартовая страница и другие
        HTML-файлы по умолчанию |

        ✅ Дополнительно
        * Если вы хотите использовать HTTPS — настройте Let's Encrypt с помощью Certbot.
        * При работе с виртуальными хостами — создавайте отдельные .conf файлы в /etc/nginx/conf.d/.
        * Для отладки используйте curl, например:
        ```bash
        curl -I http://localhost
        ```
    </TabItem>
    <TabItem value="ubuntu" label="Ubuntu">
        ## 🔄 Шаг 1: Обновление системы
        Перед установкой рекомендуется обновить пакеты системы до последних версий:

        ```bash
        sudo apt update && sudo apt upgrade -y
        ```

        > Эта команда обновит индекс пакетов и установит доступные обновления.

        ## 🌐 Шаг 2: Установка Nginx
        Установим веб-сервер Nginx из официального репозитория:
        ```bash
        sudo apt install nginx -y
        ```

        ## ⚙️ Шаг 3: Запуск и включение автозапуска службы
        После установки необходимо запустить службу и добавить её в автозагрузку:
        ```bash
        sudo systemctl start nginx
        sudo systemctl enable nginx
        ```
        * start — немедленный запуск службы.
        * enable — добавление службы в автозагрузку при старте системы.

        ## 📋 Шаг 4: Проверка статуса службы
        Проверим, успешно ли запустился Nginx:
        ```bash
        sudo systemctl status nginx
        ```
        > Вы должны увидеть строку с active (running), если всё работает корректно.

        ## 🌍 Шаг 5: Проверка доступности веб-сервера
        Откройте браузер и перейдите по адресу: `http://localhost`. Если установка прошла успешно, вы увидите стартовую
        страницу Nginx — это подтверждение, что сервер обрабатывает HTTP-запросы.
        ## 🔐 Шаг 6: Настройка брандмауэра (если используется UFW
        Если у вас установлен ufw (Uncomplicated Firewall), откройте порты для HTTP и HTTPS:
        ```bash
        sudo ufw allow 'Nginx Full'
        sudo ufw enable
        ```

        ## 🛠️ Шаг 7: Основные команды управления Nginx
        ```bash
        # 🔁 Перезапуск службы (используется после обновлений)
        sudo systemctl restart nginx

        # ✅ Проверка корректности конфигурации
        sudo nginx -t

        # 🔄 Перезагрузка конфигурации без остановки службы
        sudo systemctl reload nginx

        # ⛔ Остановка службы
        sudo systemctl stop nginx
        ```
        > Рекомендуется всегда проверять конфигурацию командой nginx -t перед перезагрузкой или перезапуском, чтобы
        избежать
        сбоев.

        ## 📁 Шаг 8: Структура и расположение важных файлов
        | Назначение | Путь | Описание |
        |--------------------------------|---------------------------------------|------------------------------------------------------------------------------|
        | Основной конфигурационный файл | /etc/nginx/nginx.conf | Главный файл настроек Nginx |
        | Каталог конфигураций сайтов | /etc/nginx/conf.d/ | Место для размещения отдельных конфигураций (виртуальных
        хостов) |
        | Виртуальные хосты | /etc/nginx/sites-available/ | Место размещения отдельных конфигураций (в некоторых ревизиях можно указывать в conf.d)
        | Активные хосты (symlinks) | 	/etc/nginx/sites-enabled/ | Место размещения конфигураций, которые в работе (symlink и виртуальных хостов)
        | Логи сервера | /var/log/nginx/access.log и error.log | Логи запросов и ошибок |
        | Публичная директория сайтов | /usr/share/nginx/html/ | Каталог, в котором лежит стартовая страница и другие
        HTML-файлы по умолчанию |

        ✅ Дополнительно
        * Если вы хотите использовать HTTPS — настройте Let's Encrypt с помощью Certbot.
        * При работе с виртуальными хостами — создавайте отдельные .conf файлы в /etc/nginx/conf.d/.
        * Для отладки используйте curl, например:
        ```bash
        curl -I http://localhost
        ```
    </TabItem>
    <TabItem value="debian" label="Debian">

        ## 🔄 Шаг 1: Обновление системы
        Перед установкой рекомендуется обновить пакеты системы до последних версий:

        ```bash
        sudo apt update && sudo apt upgrade -y
        ```

        > Эта команда обновит индекс пакетов и установит доступные обновления.

        ## 🌐 Шаг 2: Установка Nginx
        Установим веб-сервер Nginx из официального репозитория:
        ```bash
        sudo apt install nginx -y
        ```

        ## ⚙️ Шаг 3: Запуск и включение автозапуска службы
        После установки необходимо запустить службу и добавить её в автозагрузку:
        ```bash
        sudo systemctl start nginx
        sudo systemctl enable nginx
        ```
        * start — немедленный запуск службы.
        * enable — добавление службы в автозагрузку при старте системы.

        ## 📋 Шаг 4: Проверка статуса службы
        Проверим, успешно ли запустился Nginx:
        ```bash
        sudo systemctl status nginx
        ```
        > Вы должны увидеть строку с active (running), если всё работает корректно.

        ## 🌍 Шаг 5: Проверка доступности веб-сервера
        Откройте браузер и перейдите по адресу: `http://localhost`. Если установка прошла успешно, вы увидите стартовую
        страницу Nginx — это подтверждение, что сервер обрабатывает HTTP-запросы.
        ## 🔐 Шаг 6: Настройка брандмауэра (если используется UFW
        Если у вас установлен ufw (Uncomplicated Firewall), откройте порты для HTTP и HTTPS:
        ```bash
        sudo ufw allow http
        sudo ufw allow https
        sudo ufw enable
        ```

        ## 🛠️ Шаг 7: Основные команды управления Nginx
        ```bash
        # 🔁 Перезапуск службы (используется после обновлений)
        sudo systemctl restart nginx

        # ✅ Проверка корректности конфигурации
        sudo nginx -t

        # 🔄 Перезагрузка конфигурации без остановки службы
        sudo systemctl reload nginx

        # ⛔ Остановка службы
        sudo systemctl stop nginx
        ```
        > Рекомендуется всегда проверять конфигурацию командой nginx -t перед перезагрузкой или перезапуском, чтобы
        избежать
        сбоев.

        ## 📁 Шаг 8: Структура и расположение важных файлов
        | Назначение | Путь | Описание |
        |--------------------------------|---------------------------------------|------------------------------------------------------------------------------|
        | Основной конфигурационный файл | /etc/nginx/nginx.conf | Главный файл настроек Nginx |
        | Каталог конфигураций сайтов | /etc/nginx/conf.d/ | Место для размещения отдельных конфигураций (виртуальных
        хостов) |
        | Виртуальные хосты | /etc/nginx/sites-available/ | Место размещения отдельных конфигураций (в некоторых ревизиях можно указывать в conf.d)
        | Активные хосты (symlinks) | 	/etc/nginx/sites-enabled/ | Место размещения конфигураций, которые в работе (symlink и виртуальных хостов)
        | Логи сервера | /var/log/nginx/access.log и error.log | Логи запросов и ошибок |
        | Публичная директория сайтов | /usr/share/nginx/html/ | Каталог, в котором лежит стартовая страница и другие
        HTML-файлы по умолчанию |

        ✅ Дополнительно
        * Если вы хотите использовать HTTPS — настройте Let's Encrypt с помощью Certbot.
        * При работе с виртуальными хостами — создавайте отдельные .conf файлы в /etc/nginx/conf.d/.
        * Для отладки используйте curl, например:
        ```bash
        curl -I http://localhost
        ```
    </TabItem>
</Tabs>